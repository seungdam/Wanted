# Isometric 월드 소스 평가 및 고도화 순서

평가일: 2026-09-16. 기준: `game_plan.md` v1.2와 현재 작업 트리. 외부 라이브러리 내부 품질은 평가 대상에서 제외했다.

## 1. 판단

현재 코드는 **걷고 관찰할 수 있는 월드 베이스라인**이다. 투영·길찾기·이동·NPC 배회·시야·낮밤은 구현됐으나, 게임 기획의 채집→제작→거래→성벽 기록 순환은 아직 없다.

이동계와 LimboAI는 유지한다. 먼저 현재 구조를 정돈하고, 정적 대상 하나와의 상호작용을 완성한 뒤 경제 기능을 연결하는 순서가 적절하다. 월드 전체 재작성, 별도 ECS, 범용 이벤트 버스, 선제적 매니저 계층은 필요하지 않다.

## 2. 현재 실행 구조

```text
project.godot → scenes/village.tscn
  Village / village.gd
    지형 생성 → A* 통행 정보 → 건물 생성 → 플레이어 연결 → NPC 생성
    마우스 → 화면/타일 좌표 변환 → request_move → player.follow_path
  Objects [Y 정렬]
    Player / player.gd → 물리 프레임 advance → grid_position → 화면 position
    Building_* / building.gd → 임시 이미지·노멀맵
    NPC_* / npc.gd [player.gd 상속]
      BTPlayer [수동 갱신] → 휴식 → 목적지 선택 → advance
      플레이어와의 거리 → 시야 투명도
  Camera2D / follow_camera.gd → 플레이어 추적
  WorldClock / world_clock.gd → 세션 시간·태양·환경광
  HUD/TimeBar / time_bar.gd → 시간 표시
```

MCP 씬 조회에서 저장된 노드 구조와 NPC `BTPlayer.update_mode = 2`를 확인했다. 건물과 NPC 인스턴스는 실행 중 생성되므로 저장된 마을 씬 트리에는 없다. 정적 씬에 보이지 않는 것을 미구현으로 판단하면 안 된다.

| 파일 | 현재 책임 | 평가·정리 방향 |
|---|---|---|
| `scripts/village.gd` | 월드 생성, A*, 입력, 경로·시야 표시, 안내 | 조립 지점으로 유지. 거래·인벤토리 로직을 추가하지 않는다 |
| `scripts/player.gd` | 공통 이동, 방향·도착 신호, 임시 외형, 시야 설정 | 이동 알고리즘 재사용 가치가 높다. NPC가 플레이어 전용 필드까지 상속하는 구조는 역할 분화 때 정리 |
| `scripts/npc.gd` | 배회 트리 갱신, 목적지 선택, 가시성 | AI와 가시성 분리는 좋다. 대화 중 배회 중단과 트리 구조 의존을 다음 단계에서 다룬다 |
| `ai/tasks/*.gd`, `ai/trees/npc_wander.tres` | 휴식·선택·이동 순서 | 작고 명확하다. 현재 BT를 유지하며 역할별 행동이 생길 때 확장 |
| `scripts/building.gd` | 건물 표시만 담당 | 건물 종류·ID·점유 셀·접근 셀·상호작용 기능은 없다 |
| `scripts/world_clock.gd` | 세션 시간, 연수 계산, 조명 | 로컬 미리보기에는 충분. 공통 경제 시간의 기준으로 쓰기에는 부적절 |
| `scripts/time_bar.gd` | 직접 그리는 시간 HUD | 베이스라인에 충분. 경제 화면까지 직접 그리기를 확대할 필요는 없다 |
| `scripts/follow_camera.gd` | 지수 보간 추적 | 작고 독립적이다. 현재 유지 |
| `tests/baseline_test.gd` | 월드 전체 회귀 검사 | 검사 범위가 좋다. 시간 기본값과 씬 이름에 강하게 묶여 있다 |
| `demo/` | LimboAI 예제·에셋 | 주 게임과 구별하여 유지. 삭제 여부는 참조·내보내기 범위를 확인한 뒤 결정 |

## 3. 기획 대비 구현 상태

| 기획 항목 | 상태 | 남은 작업 |
|---|---|---|
| Isometric 마을 탐색 | 구현 | 실제 아트, 시설 배치, 화면 크기 대응 |
| 시간대 변화 | 부분 구현 | 생활 시간·시장 시간 정의 통일 |
| 주민 | 부분 구현 | 현재 무작위 배회만 수행. 고정 대사·역할·상호작용 없음 |
| 채집·제작·인벤토리 | 미구현 | 자원 수량, 레시피, 소모·보상 규칙 |
| 너트/볼트/벨트/베어링 | 미구현 | 최소 단위 정수 저장, 환산 표시 |
| 지갑·서명·합의 연출 | 미구현 | 교육용 표현과 실제 데이터 처리의 경계 결정 |
| 거래·장터·소유 이력 | 미구현 | 영속 데이터와 서버 확정 처리 |
| 성벽 블록 | 미구현 | 거래 ID 기반 기록 조회·연출 |
| AI 이미지/NFT·뉴스 | 미구현 | 생성/실패/재시도, 비용 한도, 승인된 이벤트 적용 |
| 모의투자·리플레이 | 미구현 | 데이터 파일, 시장 시계, 주문·폐업 규칙 |
| 피싱·복구·튜토리얼 | 미구현 | 교육 시나리오와 진행 상태 |
| 다른 플레이어 이동 | 미구현 | 접속 상태와 위치 동기화 |
| 웹 배포 | 미검증 | `export_presets.cfg` 없음. 브라우저 실제 실행 검사 필요 |

LimboAI 웹용 WASM 바이너리와 `.gdextension` 매핑은 존재한다. 따라서 웹 불가능이라고 단정할 근거는 없으며, 데스크톱 성공을 웹 성공으로 간주할 근거도 없다.

## 4. 우선순위별 평가

아래의 확장 위험은 현재 기본 씬에서 재현된 이동 실패와 구분한다. 기존 베이스라인 검사는 통과했다.

### P1: 상호작용·경제를 붙이기 전에 정할 것

1. **시간 정의 불일치.** `world_clock.gd:3–4`는 240초/하루, 5년/하루다. 기획은 600초/하루다. 또한 4주 상시 진행에서 600초마다 시장 일봉 하나를 소비하면 4,032개 일봉이다. 달력일과 거래일 중 무엇으로 세는지에 따라 기획의 ‘약 16년’ 해석이 달라진다. 생활 시각과 리플레이 인덱스를 별개로 정의하고, 연수 표시는 데이터 날짜에서 계산하는 방향을 권한다. 이번 정리에서는 임의로 기본값을 변경하지 않았다.

2. **상호작용 대상과 이동 목적지가 구별되지 않는다.** `village.gd.request_move()`는 막힌 셀을 거부한다. 건물 셀을 클릭하는 것만으로는 시설 사용에 도달할 수 없다. 대상 ID와 접근 가능한 셀을 보관하고, 경로가 실제 존재하는 접근 셀을 선택해야 한다. 가장 가까운 셀 하나만 고르면 장애물 뒤 시설에서 실패할 수 있다.

3. **도착 신호의 상태 확정 순서.** `player.gd.advance()`는 루프 안에서 `arrived`를 발생시키고, 화면 `position`은 루프 뒤에 갱신한다. 현재 수신자는 상태 문구만 바꾸므로 문제없지만, 이후 수신자가 물리적 거리·위치로 상호작용 가능 여부를 판단하면 직전 위치를 읽을 수 있다. 상호작용 추가 시 위치 갱신 후 신호를 발생시키는 계약을 세우고, 신호 콜백 안에서 위치가 최종 셀과 일치하는지 검사한다.

4. **좌표계 전제가 암묵적이다.** `player.gd.advance()`는 Ground의 로컬 좌표를 Objects 자식의 `position`에 바로 넣는다. 경로선·시야선·건물도 비슷한 전제가 있다. 기본 씬의 Ground/Objects 변환은 같아서 통과한다. 맵을 재배치할 때 둘의 변환이 달라지면 어긋날 수 있으므로, 동일 월드 좌표계 규칙을 문서화하거나 `to_global/to_local` 변환을 해당 경계에 적용한다. 타일 셀, 연속 그리드 위치, 화면 좌표를 경제 데이터에서 혼용하지 않는다.

5. **공유 경제에 사용할 시간·상태 권한이 없다.** 현재 시간은 실행 세션마다 0부터 시작하며 Inspector로 변경 가능하다. 거래 결과와 공동 뉴스는 로컬 시계나 애니메이션 완료가 확정해서는 안 된다. 향후 서버 확정 기록을 화면에 반영하고, 로컬 시계는 표시·조명 용도로 제한하는 경계를 마련한다.

### P2: 현재 코드에서 보이는 확장 비용

- `npc.gd:23`의 `get_root_task().get_child(0)`은 첫 자식이 휴식 노드라는 전제다. 트리 편집 시 디버그 대기 조절이 무력화되거나 구조에 따라 오류가 날 수 있다. 역할별 트리를 도입할 때 해당 노드 참조/대기 설정 계약을 명시한다.
- 고정 `NPC_SPAWNS`는 현재 유효하지만 필터 없이 우선 사용된다. 맵 데이터가 바뀌면 고정 스폰도 `is_walkable()`로 검증해야 한다. A* 장애물과 NPC의 목적지 목록은 시작 때 구성되므로 동적 건축·자원 제거는 별도 갱신 규칙이 필요하다.
- 건물은 단일 셀 점유이며 바닥 원점에 `Vector2(0, 14)` 보정이 있다. 아트 도입 시 점유 영역, 발 기준 Y 정렬, 이미지 오프셋, 클릭 영역, 접근 셀을 별도로 검증한다.
- 매 프레임 시야선 65점과 전체 남은 경로를 재구성하고, 플레이어는 정지 중에도 다시 그린다. 임시 캐릭터의 몸통 스타일도 그리기마다 생성한다. 현재 성능 결함이라고 단정할 측정치는 없으며, 최대 맵/NPC와 웹에서 측정한 뒤 변경 시 갱신 또는 재사용을 적용한다.
- TimeBar는 폭 620, 하단 안내는 Y=720/752에 고정이다. 작은 뷰포트에서 잘림 가능성이 있어 브라우저 크기별 검사가 필요하다. 실제 UI를 붙일 때 Container·앵커와 키보드 접근을 사용한다.
- 시야는 거리 페이드이며 벽 차폐가 아니다. NPC끼리 통과하는 것도 현재 명시된 동작이다. 이를 버그로 고치기 전에 게임에서 필요한지 결정한다.

### P3: 문서·검증 유지보수

- README의 256타일, 대문자 시간 필드, 외부 의존성 없음 설명을 현재 코드와 맞췄다.
- `baseline_test.gd`의 시간 검사는 240초에 고정되어 있다. 생활 시간 기본값 변경 시 기본값 검사와 변환 계산 검사를 구분하고 함께 갱신해야 한다.
- LSP는 `player.gd:37`의 float→int 축소 변환 경고 2건, `world_clock.gd:72`의 정수 나눗셈 경고 1건을 반환했다. 현재는 부호(-1/0/1)와 시각 분리이므로 의도가 분명하지만, 후속 정리에서 명시적 변환과 의도 주석으로 정돈할 수 있다.

## 5. 이번에 적용한 정리

- `village.gd._ready()`를 읽을 수 있는 조립 순서로 정리했다. A* 초기화, 건물 생성, NPC 생성을 같은 파일의 함수로 나눴다. 외부 서비스나 새 추상화는 추가하지 않았다.
- 노드 이름, 이동 규칙, 시간 기본값, 기획 원문은 유지했다.
- README를 실제 구현·검사 범위와 맞추고 이 평가 문서를 연결했다.
- 기존 사용자 변경과 플러그인·임포트 파일은 되돌리지 않았다.

## 6. 다음 개발 순서와 완료 조건

| 단계 | 최소 구현 범위 | 완료 조건 |
|---|---|---|
| 0. 실행 기준 고정 | 현재 회귀 검사 유지, MCP 실행 경로 정정, 웹 실행 점검, 시간 정의 결정 | 같은 코드로 재현 가능. 웹에서 월드·NPC·입력 확인 |
| 1. 상호작용 한 종류 | 자원 대상 클릭→접근 셀 이동→채집→수량 표시 | 먼 거리/도달 불가/이동 재지정/중복 클릭/대상 소멸 시 잘못된 보상 없음 |
| 2. 작은 생산 순환 | 자원 1종, 레시피 1개, 작업대 1곳, 소지품 UI | 제작 전후 수량 보존, 재료 부족 처리, 취소 시 무소모 |
| 3. 거래와 기록 | 두 사용자 간 일반 아이템 거래 한 건, 잔액·소유권·거래 ID, 성벽 표시 | 중복 요청에 한 번만 처리, 잔액/재고 부족 거부, 재접속 후 동일 기록 |
| 4. 첫 교육 흐름 | 지갑 설명→채집→제작→서명 확인→거래→성벽 내역 | UI 행동과 학습 개념이 한 번에 하나씩 연결되고 진행 상태 복구 가능 |
| 5. AI·시장 확장 | AI 아이템 1종과 소유 이력, 제한된 뉴스 이벤트, 작은 리플레이 데이터 | 실패·비용 한도·중복 적용 방지 검증 후 종목과 이벤트 수 확대 |
| 6. 피싱·복구 확장 | NPC 시나리오 1개, 설명, 복구 퀘스트 | 피해뿐 아니라 복구까지 완료하고 기존 튜토리얼과 통합 |

단계 1의 상태 흐름은 `대기 → 접근 중 → 실행 가능 → 처리 중 → 완료/실패`로 충분하다. 새 이동 명령은 기존 상호작용 의도를 취소하고, 도착 시 대상 유효성·접근 거리·실행 조건을 다시 검사한다. UI를 연 상태에서는 월드 클릭을 소비한다. 초기에는 대상 ID와 셀, Godot signal, 작은 스크립트로 구현한다.

거래 성벽은 확정 거래 ID를 기준으로 표현한다. 연출을 다시 재생하거나 씬을 다시 열어도 거래가 중복 생성되어서는 안 된다. 기록 개수만큼 무한히 물리 노드를 쌓는 설계 대신, 표시 가능한 벽 구간과 내역 조회를 분리하는 방향이 적절하다.

채집과 제작은 로컬 프로토타입으로 검증할 수 있지만, 플레이어 간 재화 이동을 추가하는 단계부터는 서버에서 수량과 권한을 검증해야 한다. 실제 플레이어 거래를 NPC 상점으로 대체한 것을 기획 완료로 계산하지 않는다.

## 7. 기획에서 먼저 합의할 항목

1. 10분은 생활 하루인가, 시장 일봉 하나인가? 오프라인 동안에도 시장은 진행하는가?
2. 지갑·서명·NFT는 교육용 시뮬레이션인가, 실제 체인 연동인가? 현재 기획에는 체인·키 보관 방식이 지정되지 않았다. 우선 교육용 내부 기록으로 범위를 명시하는 안을 권한다.
3. 채집 자원은 개인별인가, 마을 공유인가? 재생 주기와 보상 확정 시점은 언제인가?
4. 첫 시연에 필요한 아이템·레시피·시설을 각각 하나로 고를 수 있는가?
5. 단말기 고가 모델의 ‘빠르고 수수료가 낮음’과 기획 안에서 비교한 ‘수수료를 많이 내면 빠름’은 어떤 교육 설명으로 연결할 것인가? 성벽 블록과 실제 블록의 차이도 단순화임을 명시해야 한다.
6. AI 뉴스 ‘하루 0~2회’는 현실 하루인가 게임 하루인가? 후자는 10분 기준 현실 하루 최대 288회이므로 비용·이벤트 밀도가 크게 달라진다.
7. 튜토리얼은 제목의 10단계인가, 목록의 11단계인가? 마지막 장면을 별도 마무리로 정리할 수 있다.
8. 실제 사용자 이동 동기화와 피싱 협동은 첫 거래 흐름 완성 뒤로 미룰 것인가? 현재 미정인 항목을 확정 요구사항처럼 구현하지 않는다.

## 8. 검증 결과와 한계

- Godot MCP: 에디터 연결 확인, Village 씬 트리와 NPC BTPlayer 속성 조회, 프로젝트 구조 지도 생성(`res://scripts` 7개 스크립트/3개 연결), 게임 런타임 ping 성공.
- MCP LSP: 월드 7개 스크립트와 BTAction 2개 확인. 위의 경고 3건 외 진단 없음. 지도 범위를 벗어난 `ai/tasks`는 별도로 읽고 검사했다.
- 실제 엔진: `D:\Godot\Godot_v4.7.1-stable_win64.exe`에서 정리 전후 baseline 실행. NPC/SOLAR/DEBUG/CLOCK/BASELINE의 다섯 PASS 메시지 확인.
- MCP `project_info`는 잘못 지정된 `C:\Program Files\Godot\Godot.exe` 때문에 실패했다. `resource_dependencies`와 `export_presets`는 빈 응답이라 성공 근거로 사용하지 않았다. 의존성과 내보내기 파일 유무는 파일 내용으로 확인했다.
- 최초 기본 로그 경로 실행은 signal 11로 종료됐다. 로그를 작업 공간 경로로 지정한 재실행에서는 통과했다. 충돌 원인을 확정하지 않았으며 종료 코드만으로 성공을 판단하지 않았다.
- 정상 실행에서도 Windows 인증서 저장소 읽기 오류가 출력됐다. 로컬 검사는 통과했으나 HTTPS 통신은 별도 검증이 필요하다. 동시 테스트 실행 때의 MCP 7777 포트 경고는 별도로 실행한 월드가 포트를 사용했기 때문이다.
- 이번 검증은 headless 실행이다. 새 스크린샷, 시각적 가림 품질, 실제 FPS, 브라우저 실행, 네트워크 거래, 영속 저장 성공을 검증한 것은 아니다.

재현 명령(프로젝트 루트, 엔진 위치는 환경에 맞게 변경):

```powershell
& 'D:\Godot\Godot_v4.7.1-stable_win64.exe' --headless --path . --log-file ./tests/review-runtime.log --scene res://tests/baseline_test.tscn
```

로그는 검증 부산물이다. 테스트 통과는 현재 이동 월드의 보존 근거이며 기획 전체의 완료를 의미하지 않는다.
