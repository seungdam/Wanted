# Bit & Bloom — 구현 대조 및 다음 콘텐츠 설계·실행 계획

작성: 2026-09-19 · 상태: 코드 조사 결과 + 미확정 설계 제안

## 1. 문서의 기준

루트에 정확히 `design.md`라는 파일은 없었다. 현재 디자인 기준은 [demo_design_plan.md](demo_design_plan.md)이며, [game_plan.md](game_plan.md), [game_plan_v2.md](game_plan_v2.md), [TRADE_IMPLEMENTATION_PLAN.md](TRADE_IMPLEMENTATION_PLAN.md)와 이후 사용자 합의를 함께 대조했다.

우선순위는 **최근 사용자 확정 > demo_design_plan의 합의 > 충돌하지 않는 v2 > 초기 초안**이다. 코드의 현재 동작을 기획 확정으로 간주하지 않는다. 이 문서는 소스 수정 결과가 아니라 조사와 후속 구현 계획이다.

- 확정: 게스트 플레이, 생활 주민 4명, 호감도 아이템 10종과 주민별 선호 2~3종, 선택적인 튜토리얼, 비호감 선물의 관계 하락·거래 불이익, 주민 범위 정리 후 경제 콘텐츠 진행.
- 문서상 최신 합의: 5일 체험, 성벽 대신 추억 그래프, 과거 가격 리플레이, 고정 후보 안의 API 사건 선택, 관계 회고 후 경제 개념 연결, OpenAI 최종 보고서.
- 추가 확인: ‘최대 2번’의 적용 단위와 ‘제작 재료 → 호감도’가 부탁의 비용인지 보상 우선순위인지. 현재 구현은 각각 ‘주민별 하루 거래 2회’, ‘재료 소모 후 호감도만 지급’이다.
- 이번에 새로 확정하지 않음: 첫 사건, 종목 수 축소, API 공급자, 하루 길이, 선물 횟수, 부탁 보상량.

### 현재 대사 시트에 대한 즉시 결정안

- `npc.trade_cancel`은 **AB열을 정식 열**로 사용한다. N열의 기존 중복 값은 비우고, 파서가 마지막 열을 덮어쓰는 동작에 의존하지 않는다.
- 주민 이름은 현재 구현의 `리벳·모아·포포·보리`를 유지한다.
- 새 성격 매핑은 `리벳=알뜰(fixer)`, `모아=호기심(gardener)`, `포포=겁쟁이(careful)`, `보리=호탕(merchant)`으로 제안한다.
- `npc.{role}.speaker` 열은 네 역할 모두 추가한다. 주민 이름 표시의 단일 원천은 NPC profile이며, 시트의 speaker 값은 대사 선택·검수용 보조 데이터로 사용한다.
- 이 매핑을 확정하면 Godot profile, `scenarios.xlsx`, Inspector 기본값, 대사 검증 테스트에 동일하게 적용한다.

## 2. 종합 판정

Godot은 **월드 + 주민 거래·선물·부탁의 플레이 가능한 초기 구현**이다. Python은 **별도로 실행되는 경제 시뮬레이션 코어**다. 둘 사이에 상태 공유나 경제 API 연동은 없다. 최신 디자인의 전체 생활·경제·엔딩 루프가 완성된 상태는 아니다.

기존 구현 계획의 ‘생활 거래 구현 완료’는 과대 표기였다. 채집·제작·가구 구매 연결은 미구현이다. 원장도 주민 행동의 기초까지만 구현되어 있다. 테스트 통과는 현재 테스트의 기대값을 충족한다는 뜻이며 기획 일치나 모든 UI 경로의 완성을 보증하지 않는다.

## 3. 기능별 대조표

| 항목 | 디자인/대화 기준 | Godot 현황 | Python 현황 | 판정 및 다음 작업 |
|---|---|---|---|---|
| 진입 | 타이틀→게스트·닉네임→시나리오→마을 | 관련 씬·스크립트 있음 | 18단계 튜토리얼 및 시뮬레이션용 자동 진행 | 서로 다른 흐름. OAuth 재도입 불필요 |
| 월드 | 2.5D, WASD, 시야 등장, NPC 근접 상호작용 | 구현, 월드 기준 테스트 있음 | 월드/UI 없음 | Godot 유지 |
| 주민 4명 | 취향·분위기·관계·선택 활동 | 프로필 4개, 입주일 기본 1·2·3·4일 | 개별 관계 주민 없음 | 주민 시스템은 Godot 고유 구현 |
| 선물 10종 | 각 주민 선호 2~3종 | 데이터와 선물 선택 있음 | 동일 아이템/취향 모델 없음 | 데이터 구현, UI·경계 조건 미완 |
| 거래·역제안 | 가격 수락·역제안·취소 | 별도 거래 위젯, 드롭·이동 연출·가격 협상 | 시장/상점 거래, 주민 협상 없음 | 가격 패널티·역제안 범위 수정 필요 |
| 부탁 | 재료와 호감도 연결 | 고정 재료 1개 전달, 주민별 하루 1회, 호감도 +4 | 관리소 심부름→돈·평판 보상 | 요구 해석 확인 필요 |
| 난수·AI | 페르소나·기분·사건·관계가 반응에 영향 | 일별 seeded 기분, 대사 선택. LimboAI는 배회 | seeded 시뮬레이터 및 사건 점수 선택 | 거래 의사결정 BT/LLM은 없음 |
| 데이터 편집 | Inspector 또는 Excel, 모든 대사 XLSX | Inspector 필드, Excel 도입. 일부 대사 하드코딩 | Config dataclass·JSON | 편집값 적용 버그 및 대사 이관 필요 |
| 인벤토리 | 일관된 아이콘·분류·수량 | 세 기본 재료+곡괭이 중심 목록, 선물은 10종 | 개별 Item 목록 | 화면 간 아이템 목록 불일치 |
| 채집·제작·가구 | 재료 사용의 선택과 방 꾸미기 | 도구 준비 안내 수준, 실제 루프 없음 | 광석·과일·낚시·농사·집 수리·NFT 제작 함수 | 일반 가구 제작·방 배치는 양쪽 미구현 |
| 도감·매매 기록 | 수집, 최고·최저 판매가, 엔딩 업적 | 전용 도감·집계 없음 | 거래 로그는 있으나 동일 도감 없음 | 원장 기반 파생 집계 필요 |
| 추억 액자 | 꽃 홈에 블록 등록, 개별 기록·업적 | 기록을 꽃 둘레에 자동 배치, 클릭 조회 | 거래→성벽 블록 | 수동 등록·배치 저장·업적 판정 없음 |
| 사건 | 고정 후보, API 선택, 가격 직접 생성 금지 | calm_day와 수요 설정 함수뿐 | 29개 카드, 규칙 기반 선택, 뉴스 2회 | Godot 사건 실행기와 AI 연결 미구현 |
| 주식 | 과거 데이터 재생·보유·손익·수수료 | 투자 관련 거래 타입 이름만 존재 | 11개 자산, 매수·매도·평균가·수수료·리플레이 | 핵심 공식 재사용 가능, 연결은 새 작업 |
| 시간·종료 | 마을 5일 후 회고, 역사 시간 분리 | 240초/일, 5년/일 계산 잔존, 5일 종료 없음 | 별도 장 시간·취침, 성벽 달성 시 조기 종료 가능 | 양쪽 모두 최신 종료 규칙과 불일치 |
| 저장 | 지속되는 관계·행동·회고 | 세션 메모리, 영속 저장 없음 | JSON 직렬화/복원과 파일 함수 | Godot 저장 추가, NPC 상태 포함 |
| 엔딩 | 관계→거래 보고서→경제 개념 | 전용 엔딩 없음 | 손익·사건·성벽 요약, 고정 텍스트 | 관계 회고와 OpenAI 보고서 없음 |
| NFT/경매 | AI 이미지·수집·소유 기록 | 타입 이름만 있음 | Item 생성·규칙 입찰·낙찰·이력 | Python도 이미지 생성/실제 체인 없음 |
| 피싱·대출 | 이전 초안 범위, 깊이 미정 | 지갑 안내와 별개로 실제 경제 기능 없음 | 피해·복구·대출·이자·상환 있음 | 우선순위 확정 전 일괄 이식하지 않음 |

주요 코드 근거: [GuestSession](scripts/guest_session.gd), [NPC](scripts/npc.gd), [거래 위젯](scripts/trade_widget.gd), [선물/부탁](scripts/gift_widget.gd), [링 메뉴](scripts/ring_menu.gd), [추억 그래프](scripts/memory_graph.gd), [시계](scripts/world_clock.gd), [LimboAI 트리](ai/trees/npc_wander.tres).

## 4. 경제 구현 전에 해결할 코드 문제

### P0 — 수치의 의미와 편집값

1. **비호감 패널티 방향이 반대다.** `NPC.receive_gift()`와 `refresh_trade_request()`가 `request_price`에 패널티를 더한다. 이 가격은 NPC가 플레이어에게 지급하는 매입가이므로 비호감 선물 뒤 플레이어의 수익이 증가한다. 반복 선물 시 당일 가산도 누적된다. `trade_test.gd`도 가격 증가를 정답으로 검사하므로 현재 테스트가 문제를 고정한다.
   - 수정 기준: NPC 매입가는 감소하고 역제안 허용 범위도 불리해져야 한다. 반복 가산 대신 기초 조건에서 현재 패널티를 계산한다. 만료 시 정상 복귀하며 최소 가격은 유지한다.
2. **호감도 Inspector 값이 실제로 덮어써지지 않는다.** `configure_trade()`의 `affection_weights.merge(weights)`는 기존 키를 덮어쓰도록 지정하지 않았다. 이미 있는 trade/gift/quest 기본값은 유지된다. 기존 테스트는 같은 기본값 1/3/4로만 확인한다.
   - 수정 기준: 2/5/7처럼 다른 값을 지정하고 실제 변화량까지 검증한다. 설정 전용 키와 호감도 가중치를 혼합할 필요도 없다.
3. **실제 호감도 변화와 표시·원장 정보가 다르다.** `gain_affection()`은 상한 20에 걸려도 설정 보상 전체를 반환한다. 비호감도 0에서 -3으로 표시되지만 상태는 0이다. 선물·부탁 원장에는 최종 호감도 전후/실제 차이가 없다.
   - 수정 기준: 전후 차이를 결과와 원장에 기록한다. 회고는 실제 변화만 사용한다.

### P1 — 흐름과 데이터 일관성

| 문제 | 근거 | 개선 기준 |
|---|---|---|
| 제한 범위를 임의로 넓혀 구현 | `can_trade_today()`의 주민별 일일 카운트 | 튜토리얼 최대 2회와 일반 거래 제한을 사용자 답변 후 구분 |
| 선물 반복으로 관계 즉시 최대 가능 | GiftWidget 재진입마다 지급 가능, 주민 선물 제한 없음 | 반복 선물 감쇠/하루 1회 보상 중 설계 선택. 제한을 임의 확정하지 않음 |
| 가격이 관계 변화와 즉시 일치하지 않음 | 가격 갱신은 일별, 관계는 즉시 변화 | 제안 생성 시 조건 스냅샷, 다음 대화에서 재계산 |
| 같은 날 사건 변경이 NPC에 반영되지 않을 수 있음 | `refresh_trade_request()`의 같은 일자 조기 반환 | 사건 revision 또는 명시적 갱신 경로 |
| 모든 주민이 같은 사건 아이템을 요청 | 전역 `demand.item`이 개인 선호를 덮음 | 주민별 사건 영향·선호 후보 가중치 적용 |
| 역제안 UI가 10~18볼트로 고정 | `_change_asking_price()` | 실제 제안·최소가·협상 범위를 기준으로 조작 범위 계산. 현재 테스트는 값을 직접 넣어 UI 제한 우회 |
| 인벤토리에서 선물 7종 누락 | `_show_items()` 고정 목록 | 같은 아이템 카탈로그에서 거래·선물·인벤토리 구성 |
| 신규 아이템이 서로 구분되지 않는 기본 원 아이콘 | `ItemIcon._draw()`의 기본 분기 | 10종의 식별 가능한 cozy 아이콘 지정 |
| 새 위젯은 에디터에서 수정 어려움 | 거래·선물·링 상세는 런타임 생성 | 기존 대화 씬처럼 `.tscn`/Theme로 레이아웃 노출, 동적 데이터만 코드 처리 |
| 모든 대사가 XLSX를 사용하지 않음 | `gift_reply`, `complete_request`, `counter_offer_reply`, 위젯 문장 | 주민 발화 코드를 시트로 이동. 버튼/시스템 문구와 구분 |
| 모든 추억에 같은 업적 표시 | 링 메뉴에 ‘업적: 첫 거래의 추억’ 고정 | 종류별 상세와 실제 판정된 업적만 표시 |
| 꽃 노드가 많아질수록 겹침 | 고정 반지름 66, 항목 수만 증가 | 액자 1페이지 수량 제한+페이지/꽃 추가. 배치 데이터는 기록 ID로 참조 |
| 월드 이동 잠금과 시간 잠금이 별개 | `WorldClock`은 항상 처리, UI 중 시간 계속 흐름 | 대화·거래 중 일자 변경 정책 확정 후 거래 스냅샷 만료 처리 |
| 거래 성공·관계 갱신이 서로 다른 호출 | 위젯에서 `sell_item()` 후 `complete_trade()` | 경제 동작 공통 함수에서 조건검사→소모/지급→관계→원장을 한 번에 처리 |

`ScenarioSheet`는 첫 워크시트의 특정 XML 접두사(`x:`)를 정규식으로 읽는다. 현재 파일 성공과 Excel 등 다른 프로그램에서 재저장한 파일의 호환성은 별개다. 재저장된 실제 XLSX로 검증하고 필요하면 Godot XMLParser로 읽는다.

## 5. Python에서 가져올 것과 다시 설계할 것

| Python 소스 | 재사용 가치 | 이식 시 수정/확인 |
|---|---|---|
| `gwangbu/market.py` | 기준가 × 과거 종가 비율, 평균 취득가, 장 시간, 잔액/보유량 검사 | Godot과 화폐 단위·거래 타입 통일, 수수료 포함 손익 분리 |
| `gwangbu/prices.py` | CSV 가격 소스, 이전 종가 조회, 재현 가능한 합성 자료 | 결측·상장 전·구간 밖 처리를 확정하고 합성 자료는 테스트용 표시 |
| `gwangbu/events.py` | 행동 요약, 사건 중복·시간순서 필터, 기본 선택기 | API 호출 아님. 같은 유형/위기 제한은 후보 없을 때 완화하는 코드이므로 최신 정책과 대조 |
| `gwangbu/clock.py` | 개장→뉴스1→뉴스2→마감→취침 신호 | Godot의 마을 낮밤과 역사 리플레이 인덱스를 분리 |
| `gwangbu/save.py` | 버전 있는 JSON 저장·복원 | 저장 정책 상수만으로 자동 저장이 실행되지는 않음. 실제 호출 지점 및 원자적 파일 교체 필요 |
| `gwangbu/summary.py` | 투자 수익과 노동 수익 분리, 행동 근거 집계 | 성벽·고정 평가는 제외. 실제 관계 변화·도감 기록 추가, 생성 보고서 수치는 코드에서 제공 |
| `gwangbu/nft.py` | 유한 제작, 경매 입찰/정산 | 이미지 생성 API·방 꾸미기는 별도. 판매 후에도 아이템 이력 보존 필요 |
| `gwangbu/economy.py` | 채집 한도·재료 소모·수리 개념 | 10종 카탈로그와 부탁 보상 규칙에 맞춰 재작성 |
| `gwangbu/ledger.py`, `game.py` | 거래 ID·시각·종류, 하루 정산 | 성벽 모든 금전 거래 집계·조기 종료를 추억/5일 규칙으로 대체 |

Python을 완성된 서버로 간주해 통째로 연결하지 않는다. **제안:** 데모의 경제 상태는 Godot 한 곳에서 관리하고 Python은 공식·데이터 대조용으로 사용한다. 외부 AI가 필요한 시점에만 비밀키를 보관하는 서버 경유 호출을 추가한다. 두 언어에서 잔액을 동시에 변경하지 않는다.

### 데이터와 계산상 주의점

- Python은 **100너트=1볼트 정수**이고 Godot은 **정수 볼트**다. 500을 그대로 옮기면 의미가 100배 달라진다. 소수 수량/수수료 이식 전에 단위를 결정해야 한다. 제안은 내부 너트 정수·UI 볼트 표시다.
- Python 거래 타입 `stock_buy/stock_sell/nft_sold`와 Godot 타입 `investment_buy/investment_sell/nft_sale`은 다르다. 사건·추억·엔딩에서 누락되지 않도록 하나로 매핑한다.
- `market._floor_qty()`는 step 인자가 있어도 0.01 단위를 고정 사용한다. 판매는 과다 요청을 보유량으로 자동 축소하며, 결측 가격·비정상 숫자 방어도 이식 시 재검토한다.
- `ledger.debit()`는 음수 금액을 거부하지 않는다. `gather_ore/gather_fruit`도 음수 요청 방어가 없다. 기존 테스트 통과만으로 외부 입력에 안전한 API라고 판단하지 않는다.
- 사건 데이터는 29개이고 가격 카탈로그는 11개다. `coverage.csv` 기준 국내 지수는 1996년 말부터, 여러 국내 기업은 2000년 이후부터이며 1980~2025 전체 자료가 아니다. 데이터에는 2026년 5월까지도 포함된다.
- `coverage.csv`에 채권은 금리 기반 총수익 근사, 금은 월간 보간/일부 환율 고정이 명시되어 있다. 모든 시계열을 실제 종가 그대로라고 설명할 수 없다. 이번 조사는 로컬 출처 메모 확인이며 외부 원자료 정확성 감사는 수행하지 않았다.
- `python/data`와 `python/python/data`의 prices.csv·events.json은 현재 해시가 동일하다. 실행 기준은 `python/python`으로 정리하고 추후 단일 데이터 위치를 사용한다.
- 카드의 `expected`·`feedback`은 작가 초안/합성 데이터에도 쓰인다. 실제 CSV 결과나 플레이어 성과의 사실로 출력하지 않는다.

## 6. 다음 게임 디자인 제안

### 6.1 공통 생활 루프

**아침 소식 → 주민의 생활 변화 발견 → 팔기·선물·부탁·보유 중 선택 → 선택적으로 투자 → 저녁에 실제 결과 회고.**

주민 콘텐츠 분량을 더 늘리기보다 경제 사건이 기존 네 주민의 반응을 바꾸도록 연결한다. 투자하지 않은 플레이어도 생활 기록과 관계 회고로 5일을 완주할 수 있어야 한다.

### 6.2 사건을 생활과 투자에 함께 연결

사건마다 아래 데이터를 작성한다. 역사적 가격과 가상 마을 생활 효과는 연결된 연출이되 별개의 계산 근거다.

| 영역 | 데이터/표현 |
|---|---|
| 사건 정체 | ID, 실제 구간, 주제, 난이도, 후보 조건 |
| 마을 소식 | 뉴스1/뉴스2, 고정 기본 대사, 주민별 반응 code |
| 생활 효과 | 주민별 수요 아이템·수요 보너스·부탁 변화. 게임용 수치임을 구분 |
| 투자 효과 | 재생할 CSV 구간과 종목. AI가 가격 곡선을 생성하거나 가산하지 않음 |
| 근거 기록 | 사건 ID·단계·뉴스 열람 시각·행동 ID·거래 당시 조건 |
| 회고 | 실제 선택과 결과, 관련 개념, 확인 질문 |

예시 제안: ‘마을 주문이 늘어난 날’에는 리벳이 수리 재료를 찾고 보리는 물류를 걱정한다. 플레이어는 같은 재료를 판매하거나 부탁에 쓰거나 남겨 둔다. 투자 화면에서는 연결된 기업/바구니의 준비된 역사 구간을 관찰한다. 이 예시는 특정 실제 사건을 이미 선정했다는 뜻이 아니다.

‘가뭄이면 작물이 반드시 오른다’, ‘좋은 뉴스면 주식이 오른다’를 고정 정답으로 만들지 않는다. 생활 재료의 공급 감소 연출과 기업의 비용·수요·과거 가격 결과를 구분해 보여준다.

### 6.3 최소 투자 위젯의 실제 조작 순서

고정 시설 접근 → 투자 패널 → 종목 선택 → 현재까지의 차트/보유 수량 → 매수·매도 선택 → 수량 → 단가·수수료·총액 확인 → 확정 → 잔액/보유/원장 갱신.

- 취소는 자산 변화 없음. 부족한 현금·보유량·거래시간 밖은 이유와 함께 차단한다.
- 확정 중 버튼 중복 입력을 차단하고 거래 ID 하나만 생성한다.
- 뉴스 알림은 즉시 매수를 강요하지 않는다. 뉴스 읽기, 유지 선택, 거래를 서로 다른 행동으로 기록한다.
- 차트에는 재생 시점 이후의 가격을 숨긴다. 미래 고점/저점 공개로 선택이 무의미해지지 않게 한다.
- `.tscn`에 패널·슬롯·라벨·차트 영역을 배치하고 프로젝트 TTF/Theme를 적용한다. 실제 장면에서 스크린샷으로 오버플로와 클릭 영역을 검증한다.

### 6.4 도감과 추억의 역할 분리

도감은 아이템의 **발견/소유/판매 경험**, 추억은 **누구와 어떤 선택을 했는지**를 기록한다.

- 아이템별 첫 획득, 누적 획득/판매, 최고·최저 체결 단가, 해당 거래 ID·상대·사건을 보존한다.
- 미판매의 최고/최저는 0원이 아니라 ‘판매 기록 없음’. 선물·부탁의 0원 기록은 판매 최저가에 포함하지 않는다.
- 수량이 다른 거래도 단가로 비교한다. 수수료가 있으면 체결 단가와 순수령액을 구분한다.
- 업적 제안: 처음 발견, 첫 선물, 첫 부탁, 같은 물건의 서로 다른 거래 기록. 최저가 판매를 실패 낙인이나 감점으로 쓰지 않는다.
- 추억 꽃은 모든 기록을 보관하되 전시할 기록을 홈에 선택 등록한다. 거래 취소는 기록·노드 없음. 등록/해제는 원본 원장을 삭제하지 않는다.

### 6.5 선택의 재미를 해치지 않는 데모 균형

초기 풍부한 재화는 테스트용으로 유지하되 배포용 잔액과 구분한다. 모두 무료처럼 느껴지는 상태로는 판매·선물·투자의 기회비용을 검증하기 어렵다.

새 주민/선물 종류를 늘리기보다 한 사건에서 ‘같은 자원의 사용처’가 달라지는지 검증한다. 선물 연타, 모두 같은 요청, 거래량만으로 최고 평가를 받는 구조는 관계 발견과 교육 목적을 약화시킨다.

## 7. 구현 순서와 완료 기준 — 아직 미구현인 후속 계획

| 순서 | 작업 | 독립 테스트/완료 기준 |
|---|---|---|
| 0. 기존 주민 마감 | 패널티 부호, Inspector 가중치, 실제 호감도 차이, 제한 해석, 10종 목록·아이콘, 대사 외부화 | 기본값이 아닌 가중치, 0/20 경계, 반복 선물·다음날, 잘못된 부탁·중복 완료, 취소 시 불변 |
| 1. 경제 단위 테스트 씬 | `tests/economy_test.tscn`(신규 예정), 화폐 단위/보유/체결 모델, 고정 소형 가격 자료 | 매수·매도·수수료·평균가·잔액 부족·중복 확정·결측 가격·상장 전·미래 차트 미노출 |
| 2. 고정 사건 실행 | API 없이 카드1개→뉴스1/2→가격 재생→주민 반응 | 단계 순서, 하루 1사건, 일중 변경 반영, 중복 적용 없음, 해당 사건 원장 기록 |
| 3. 투자 UI 연결 | 에디터 편집 가능한 독립 Widget, 고정 시설 진입 | 실제 버튼·수량·확정·취소 경로. 내부 값을 직접 대입하는 테스트만으로 완료 판정하지 않음 |
| 4. 저장·5일 회고 | 버전 저장, 일자/사건/보유/원장/NPC 관계 복원, 5일 종료 | 재시작 후 같은 결과, 진행중 거래 취소·정산 정책, 거래 0회여도 엔딩 |
| 5. 제한된 AI 사건 선택 | 허용 후보 ID만 요청·검증, 기본 선택기, 선택 결과 저장 | API 실패·잘못된 ID·중복·역행·후보 소진·재접속 모두 진행 가능 |
| 6. 결과 보고서 | 코드 집계→OpenAI 설명→검증→엔딩 | 없는 거래·숫자 생성 방지, 근거 ID, API 실패 시 고정 회고 |
| 후속 콘텐츠 | 도감 전체 화면·간단 가구 제작·방 배치·AI 이미지/NFT 경매 | 경제 수직 흐름 완료 후 범위를 재합의 |

최소 데이터 계약 제안:

- 거래: ID, 일차·게임 시각, 종류, 상대, item/asset ID, 수량, 단가, 수수료, 현금 변화, 사건 ID·단계, 관계 전후, 추억 여부.
- 주민: 안정적인 resident ID, 호감도, 기분 seed/일자, 부탁 완료, 튜토리얼 진행, 패널티 만료. Node를 새로 생성해도 복원한다.
- 사건: 선택 ID, 선택 이유/방식(API 또는 fallback), 활성 단계, 가격 replay index. 재진입 시 재추첨하지 않는다.
- 한 동작의 정산: 검증 → 수량/현금/관계 변경 → 원장 기록 → 저장 → 화면 통지. 실패한 동작은 일부만 반영하지 않는다.

LimboAI 제안: 기존 배회 트리를 유지하면서 상호작용 중 정지·요청 표시 상태를 명시적으로 연결한다. 경제 체결·확률·가격 계산을 BT 틱마다 실행하지 않는다. 실시간 생성 대화는 가격·보상·호감도 결정 권한을 갖지 않는다.

## 8. 함께 확정할 질문

| 순위 | 질문 | 현재 상태 / 권장 출발점 |
|---|---|---|
| Q1 | 부탁의 ‘제작 재료 → 호감도’는 보상 우선순위인가, 전달 비용인가? | 현재는 재료 소모+호감도만. 사용자 확인 대기 |
| Q2 | 최대 2회는 튜토리얼 안내인가, 주민 거래 횟수인가? | 현재는 주민별 매일. 사용자 확인 대기 |
| Q3 | 하루가 거래·대화 중에도 진행되는가? | 제안: 핵심 모달 중 정지, 월드 탐험 시 진행 |
| Q4 | 데모 사건 후보·투자 자산은 몇 개를 사용할까? | v2의 29/11을 자동 확대 구현하지 않음. 먼저 한 사건으로 기술 검증 |
| Q5 | 첫 공통 사건과 실제 리플레이 구간은? | Python 기본은 카드2. 가뭄 등의 이전 제안은 확정 아님. 자료 가용성 확인 후 선정 |
| Q6 | 반복 선물은 어떤 제한/감쇠를 적용할까? | 제안: 하루 첫 유효 선물의 보상 중심. 추가 선물 소비/허용 여부 논의 |
| Q7 | 플레이 시간과 경제 UI 진입 시설은? | 현재 240초/일, 5일 20분은 구현 수치이며 최종 목표 미확정 |
| Q8 | AI 선택 API와 엔딩 API의 실행 환경은? | 엔딩 OpenAI 합의와 사건 공급자 미정을 구분. 먼저 오프라인 완주 |

Q1~Q2를 먼저 확정하고 주민 마감을 수행한다. Q3~Q5는 경제 프로토타입에 앞서 결정한다. 나머지는 필요한 구현 단계에서 질문한다.

## 9. 이번 검증 기록과 한계

- Godot `tests/trade_test.tscn` 재실행: PASS. 주민 판매·협상·선물/부탁 일부·링 메뉴 등을 검사한다. 새 선물 UI 전체 조작, 비기본 Inspector 설정, 실제 패널티 의미, 저장·경제 기능은 보장하지 않는다.
- Python `python -m unittest discover -s python/python/tests -v`: 20개 PASS. 가격 소스는 해당 테스트에서 `SyntheticPriceSource`다.
- Python `sim.py --csv data/prices.csv`를 `python/python`에서 실행: 4개 전략 모두 종료(exit 0). 기존 성벽 중심 종료 흐름이 남아 있다. 데이터 출처/경제 교육 정확성이 검증됐다는 의미는 아니다.
- Godot MCP `editor_status`: 브리지 6505 포트 사용 충돌, 연결 불가. 직접 Godot 실행은 가능했고 런타임 MCP 7777 포트 충돌 및 인증서 경고가 있었다. 이번에는 UI 스크린샷 외관을 새로 평가하지 않았다.
- 경제 로직·Python 코어는 변경하지 않았다. 2026-09-20 시나리오 정리에서 profile 성격과 role speaker 경로만 추가로 적용했다. 경제 규칙 결함은 위 구현 단계 0의 미완료 작업이다.

## 10. 2026-09-20 시나리오 시트 결정 및 적용

- `npc.trade_cancel` 중복은 N열을 비우고 AB열만 유지했다. 코드가 열 순서의 마지막 값에 의존하지 않도록 하는 정리다.
- 첨부 시트의 `npc.pin/nut/clip/screw.speaker`와 `.role`을 이동 주민의 정식 데이터로 사용한다. 값은 각각 핀·너트할머니·클립·나사다.
- 주민 성격은 핀=겁쟁이/fixer, 너트할머니=알뜰/careful, 클립=호기심/gardener, 나사=호탕/merchant로 profile에 반영했다. 역할 코드는 내부 안정 키로 유지한다.
- `npc.rivet` 등 나머지 ID는 고정/보조 주민 데이터로 보존한다. 이동 주민과 혼동하지 않는다.
- 거래 대화의 speaker label은 `npc.{resident_id}.speaker`를 먼저 읽고, 값이 없을 때 profile 이름으로 되돌아간다. 주민 이름의 실제 원천은 profile이다.
- XLSX의 점 구분 코드와 `{nickname}`, `{item}`, `{price}` 토큰 규칙은 유지한다.
- 위 시트 편집은 `tests/update_scenarios.mjs`로 수행했고, `N1:AF4`를 재검사해 N열 공백과 AC~AF 4개 코드를 확인했다.

다음 적용 단위는 시트의 선호 아이템·부탁 재료·성격 대사다. 현재는 코드 profile에 있는 값을 단일 원천으로 쓰고 있으므로, 같은 규칙을 시트로 옮길 때는 임의의 두 번째 데이터 포맷을 만들지 않고 기존 열 구조에 명시적인 role/item 필드를 추가한다.

## 11. 다음 질의응답과 구현 계획 갱신

### 이미 답한 항목

| 질문 | 현재 답변/적용 |
|---|---|
| `npc.trade_cancel` 중복은 어느 열을 남기는가? | AB열을 정식으로 사용하고 N열은 비웠다. 새 코드에서는 중복 열 덮어쓰기를 전제로 하지 않는다. |
| 주민 역할과 성격은 어떻게 매핑하는가? | 리벳=알뜰/fixer, 모아=호기심/gardener, 포포=겁쟁이/careful, 보리=호탕/merchant. 이름은 현재 Godot profile을 유지한다. |
| 역할별 speaker를 어디에 두는가? | AC~AF의 `npc.{role}.speaker`. UI는 시트 값을 먼저 읽고 profile 이름을 fallback으로 사용한다. |

### 다음 확정이 필요한 항목

아래 항목은 데모 전체의 수치와 루프를 바꾸므로 임의로 코드에 확정하지 않는다. 권장 기본안은 괄호 안에 적었다.

1. **부탁 보상 의미:** 재료를 플레이어가 전달한 뒤 호감도를 보상으로 받는가(권장·현재 구현), NPC가 재료를 보상으로 주는가, 제작품을 전달하는가.
2. **2회 제한 단위:** 데모 전체 유상 거래 2회인가, 주민별 하루 2회인가, 튜토리얼 안내 거래만 2회인가. 현재 코드는 주민별 하루 2회다.
3. **첫 사건:** 가뭄·흉작(권장), 환경 규제·벌목 제한, 금융시장 충격 중 하나. 첫 사건은 주민 수요와 가격 리플레이를 동시에 검증할 수 있어야 한다.
4. **반복 선물:** 하루 첫 유효 선물만 호감도 보상을 주고 이후는 대사·추억만 남길지, 보유량만큼 계속 허용할지.
5. **하루 진행:** 거래·대화 모달에서 월드 시계를 멈출지, 시계는 흐르되 거래 제안에 만료 시각을 둘지.
6. **첫 투자 범위:** 4개 대표 자산만 열어 교육 흐름을 검증할지, Python의 11개 자산을 처음부터 모두 열지.

### 응답 후 고정할 구현 순서

1. Q1~Q3 답변을 NPC profile, XLSX 문장 토큰, `GuestSession` 원장에 반영한다.
2. 패널티 부호·Inspector merge·호감도 실제 변화량을 수정하고, 기본값과 다른 Inspector 테스트를 추가한다.
3. Godot에 Python market의 최소 공식만 이식한다. 가격 CSV→사건 replay→매수·매도→수수료·평균가→원장 순서로 작은 단위 테스트 씬을 만든다.
4. 첫 사건 하나를 주민 요청과 투자 화면에 연결한다. 사건 선택은 API 없이 고정 fallback으로 먼저 완주한다.
5. 저장·5일 종료·관계 회고를 붙인 뒤 제한된 사건 선택 API를 추가한다.

현재 시트 수정은 이 계획의 데이터 정합성 단계만 완료한 것이다. 선호 아이템과 부탁 재료를 XLSX로 옮기는 작업은 위 Q1~Q3 확정 이후에 진행한다.

## 12. 2026-09-20 확장 scenarios.xlsx 적용

사용자가 제공한 `scenarios.xlsx`를 `res://data/scenarios.xlsx`로 교체했다. 실제 시트는 이름이 `scenarios`이며 물리적으로 첫 번째 워크시트라 현재 `ScenarioSheet`의 `sheet1.xml` 경로와 호환된다.

- A:LY, 337개 code 열을 확인했다. 기본 arrival·wallet·npc 거래 코드는 유지되고, 주민 인사·선물·역제안, 18단계 tutorial, 29개 event, day, ending 코드가 추가되어 있다.
- `npc.trade_cancel`은 한 번만 존재하며 AB열에 있다. N열에 중복이 없다.
- 이동 주민 ID와 역할은 `pin=fixer`, `nut=careful`, `clip=gardener`, `screw=merchant`다. profile도 이 네 ID를 사용한다.
- 이름·성격은 핀=겁쟁이, 너트할머니=알뜰, 클립=호기심, 나사=호탕으로 적용했다.
- `npc.{id}.speaker`를 거래 대화 UI에서 읽도록 연결했다. `npc.{id}.role`은 profile과 시트의 일치 검증용이다.
- tutorial/event/day/ending 코드는 시트에 로드되지만 아직 전용 Godot 화면과 실행기는 연결하지 않았다. 현재 주민 거래 테스트는 확장 시트 337개 code와 `npc.pin.speaker`를 검증한다.

### 새 시트에서 확인할 기획 이슈

성격→role 매핑은 적용하기에 적합하다. 호감도→mood 매핑은 관계 피드백을 분명하게 만드는 장점이 있다. 현재 코드의 seeded 일일 mood와 동시에 사용하면 서로 덮어쓸 수 있으므로, 다음 중 하나를 확정해야 한다.

- 권장: 호감도 구간이 기본 mood를 결정하고, seed는 같은 mood 안에서 greeting/gift/counter 문장 변형만 결정한다.
- 대안: 호감도는 mood 가중치만 바꾸고 seed가 최종 mood를 선택한다.

첫 번째 방식을 택하면 `sad(<30) → blank(30~59) → calm(60~79) → joy(80+)`가 관계의 가시적 보상이 되고, 난수성은 대사·부탁 변칙성에 남는다.

## 13. 대사 데이터 분리와 공통 manager

NPC code 89개를 `data/npc_dialogues.xlsx`로 분리하고, 전역 시나리오 파일에는 해당 열의 내용을 제거했다. 원본 workbook의 열 너비·서식을 보존하기 위해 빈 formatted column 영역은 남아 있지만, code registry에는 포함되지 않는다.

`DialogScriptManager`를 autoload로 추가했다. manager는 두 workbook을 읽어 `lines()`, `has_code()`, `reload()`를 제공하고, Title·ArrivalStory·NpcTradeDialogue가 이를 공통 사용한다. 파싱 책임과 게임 객체 책임을 분리하므로 이후 tutorial/event/day/ending controller도 같은 계층을 사용한다.

이 계층은 ECS를 그대로 도입한 것이 아니다. 대사 조회는 엔티티·컴포넌트·프레임 시스템보다 공유 데이터 서비스가 적합하다. 진행 상태가 여러 대화 주체에 병렬로 필요해질 때만 DialogueState component와 실행 system을 추가한다.
