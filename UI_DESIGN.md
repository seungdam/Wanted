# Bit & Bloom — Cozy UI 디자인 및 적용

2026-09-19 · game_design.md에서 분리한 UI 전용 문서.

## UI 방향과 Cozy 에셋 검토

검토 원본: `Downloaded Assets/GUI/CozyUI.png` (1536×1024).

판정: **시각 테마로 채택 가능하며 기존 UI 구조에 적용 가능하다.** `CozyUI.png`는 한 장의 시트이지만, 패널·버튼 상태·슬롯 상태·아이콘·초상 프레임이 알파 채널을 유지한 개별 영역으로 들어 있다.

원본에는 배경색·제목·예시 영어 대사·인물·아이템·수량도 함께 포함되어 있다. 따라서 인벤토리와 대화 예시 전체를 배경으로 사용하지 않고, Godot `AtlasTexture`로 필요한 개별 영역만 참조한다. 이미지의 128×128, margin 32 등의 표기는 StyleBoxTexture 여백 값으로 적용하며 런타임 캡처로 경계를 검증한다.

| 대상 | 기존 구현 | 적용 방식 제안 |
|---|---|---|
| 일반 인벤토리 | `scripts/ring_menu.gd`의 격자와 `cozy_meadow.tres` | 사용자 확정: 새 시안의 격자형. 빈 크림 패널, 잎 장식, 슬롯·선택 상태 적용 |
| NPC 대화 | `scenes/ui/npc_trade_dialogue.tscn`의 Bubble·Speaker·Body·Portrait | 빈 본문 패널, 화자 태그, 초상 프레임을 별도로 배치하고 기존 대사와 초상을 동적으로 표시 |
| 거래 | `scripts/trade_widget.gd`의 별도 위젯 | 동일 팔레트의 아이템 슬롯, 드롭 대상, 확인·취소 상태 사용. 대화창과 별도 화면 유지 |
| 버튼 | 기존 Theme와 상태 스타일 | 기본/hover/pressed/disabled/focus 상태를 구분. 영어가 박힌 버튼 이미지는 사용하지 않음 |

패널은 글자 없는 본문과 투명한 외곽을 가진 RGBA PNG로 준비하고 Godot의 StyleBoxTexture로 확장한다. 잎·꽃 장식은 고정 모서리 안에 보존하거나 별도 TextureRect로 분리해 늘어짐을 피한다. 슬롯·아이콘·초상은 종횡비를 유지한다. 아래 원안의 margin을 제작 기준으로 삼고 실제 분리 이미지에서 경계를 검증한다.

### 원본 디자인 시트 기준 — 2026-09-19 사용자 제공

참고 이미지: `codex-clipboard-2034eb29-398c-4bad-85ea-ddcf87e09a06.png` (대화 첨부). 이 시트는 UI 디자인의 기준이며, 이미지 속 설명만으로 현재 파일이 해당 규격의 독립 에셋으로 제작됐다고 판정하지 않는다.

| 요소 | 원안 규격 / 적용 기준 |
|---|---|
| 기준 해상도 | 1920×1080, UI scale 1.0. 디자인 기준이며 현재 프로젝트 해상도 변경을 의미하지 않음 |
| 인벤토리 | 목표 960×600, 카테고리 탭·우측 재화 캡슐·격자 슬롯. 원안은 6열×4행 예시이며 보유 용량 제한은 별도 기획 |
| 대화창 | 목표 1440×320, 좌측 초상·상단 화자 태그·본문·우측 진행 표시 |
| 패널 9-slice | 128×128, 사방 margin 32px |
| 대화창 9-slice | 128×128. 도식은 가로 32px·세로 24px, 하단 설명은 32px로 상충. 실제 원본 경계에 맞춰 제작 시 검증 |
| 버튼 / 포커스 외곽선 | 96×96, 사방 margin 24px |
| 아이템 슬롯 | 96×96, 아이콘 안전 영역 72×72, 수량 배지 별도 |
| 슬롯 상태 | Empty 기본, Hover 금색, Selected 녹색. 포인터와 선택 상태를 구분 |
| 화자 태그 / 카테고리 헤더 | 320×72 |
| 재화 캡슐 | 240×72, 아이콘과 금액은 별도 표시 |
| 초상 프레임 | 160×160, 주민 초상은 교체 가능한 별도 이미지 |
| 진행 표시 | 32×24 |
| 선택지 버튼 | 목표 520×80, 본문 대화창과 독립 컨텍스트 |
| 버튼 상태 | Normal, Hover, Pressed, Disabled, Focus. Focus는 별도 외곽선으로 표시 |

9-slice는 모서리를 보존하고 변과 중앙만 확장한다. 안내용 점선·치수·예시 영어·예시 수량은 게임 에셋에 포함하지 않는다. 본문·이름·버튼·수량은 런타임 데이터로 표시한다. 인벤토리와 대화의 원안 구조를 따르되 별도 거래 위젯과 기존 상호작용 흐름은 유지한다.

모든 글자는 `font/Moneygraphy-Pixel.ttf`로 렌더링한다. NPC 발화는 XLSX 코드로 연결한다. 기존 popup/hover/전달 애니메이션과 이동 잠금은 외형 교체 후에도 유지한다.

새 시안의 곰 초상은 스타일 참고다. 모든 주민에게 같은 얼굴을 부여하지 않는다. 아이템 아이콘도 현재 10종과 대응시켜야 하며 시안만으로 모든 종류가 준비되었다고 간주하지 않는다.

검토 범위: 원본 이미지, UI 씬·테마·스크립트 대조. Godot MCP editor_status는 connected=false였다. 이번 검토에서 UI 교체나 게임 코드 변경, 런타임 외관 검증은 수행하지 않았다.


## 이번 적용 결과

### 템플릿 활용 판정

| 요소 | 현재 구현 | Cozy UI 원본 템플릿 사용 여부 |
|---|---|---|
| 큰 패널 | `cozy_panel.png` 9-slice | 원안을 참고해 생성한 별도 텍스처. 원본 PNG 직접 사용 아님 |
| 버튼 | `cozy_meadow.tres`의 StyleBoxFlat | 원본 버튼은 96px 정사각형 아이콘 버튼이므로, 현재의 가로형 한글 텍스트 버튼에는 상태 팔레트만 재구성해 적용 |
| 아이템 슬롯 | 인벤토리·거래·선물 슬롯 | `scripts/cozy_ui_atlas.gd`가 `CozyUI.png`의 Empty/Hover 슬롯 영역을 AtlasTexture로 직접 사용 |
| 초상 프레임 | `ResidentPortrait`의 PanelContainer | 원안의 하늘색 안감·초록 테두리를 재구성. 원본 프레임 텍스처 직접 사용 아님 |
| Ring 아이콘 | `assets/ui/icons/cozy_*.png` | 시트의 아이콘 구성을 참고해 만든 별도 독립 아이콘 파일 사용 |

따라서 현재는 **Cozy UI 원안을 기준으로 재구성한 UI에서, 슬롯은 원본 시트의 투명 영역을 개별 PNG로 추출해 쓰는 UI**다. 추출본은 `assets/ui/cozy_source/`에 두고, `CozyUI.png` 전체를 배경으로 쓰지 않는다. 정사각형 원본 버튼은 이후 아이콘 전용 버튼이 추가될 때 직접 적용한다. 예시 문장·수량·주민 초상은 게임 데이터와 충돌하므로 사용하지 않는다.

- 새 시안을 참고해 글자 없는 잎 장식 패널을 생성했다. 원본 합성 시트를 직접 잘라 사용한 것은 아니다.
- `assets/ui/skins/cozy_panel.png`를 Godot importer의 size_limit=128로 불러오고 사방 32px 9-slice를 적용한다.
- `assets/ui/themes/cozy_meadow.tres`가 인벤토리와 NPC 대화의 크림·초록·금색 상태를 공유한다. 슬롯과 버튼 상태는 Godot StyleBoxFlat으로 구성한다.
- 일반 인벤토리는 6열×4행을 기본 표시하며 실제 보유 아이템을 순회한다. 항목이 늘면 스크롤된다. 빈 칸 개수는 게임의 소지 한도가 아니다.
- 전체·재료·생활·선물 탭을 누르면 목록을 필터링한다. 선물 탭은 현재 선물 가능 카탈로그를 사용하며 모든 10종이 해당된다.
- 선택 항목의 이름·수량과 보유 볼트를 표시한다. 존재하지 않는 도구는 소지품으로 표시하지 않는다.
- NPC 대화는 기존 씬과 XLSX 흐름을 유지하며 화면 아래 초상 프레임·이름 태그·본문·진행 버튼으로 구성한다. 초상 프레임은 하늘색 안감, 초록 테두리, 그림자로 구성하고 이미지 교체가 가능하다.
- 현재 프로젝트 1280×800 기준 인벤토리 860×620, 대화창 960×236으로 배치한다. 원안의 1920×1080은 디자인 참고이며 프로젝트 해상도를 변경하지 않았다.
- 대화와 거래 위젯은 같은 재사용 초상 프레임을 사용한다. 거래·선물 위젯의 갈색 Doboui 슬롯 이미지는 제거하고, 크림 바탕·초록 선택·금색 드롭 대상 상태를 적용했다.
- 패널은 이미지 생성 에셋, 아이템 10종은 코드로 그리는 식별용 아이콘이다. 원안의 모든 독립 PNG를 완성했다는 의미는 아니다.

## 검증

Godot MCP 연결 및 파일 재스캔 성공. `tests/trade_test.tscn`에서 거래 연결, 인벤토리 이동 잠금, 10종 표시, 탭 필터, 아이템 선택 검증 통과. 실제 렌더 캡처: `tests/inventory-theme-preview.png`, `tests/dialogue-skin-preview.png`, `tests/trade-widget-preview.png`, `tests/gift-widget-preview.png`.

위 '검토 범위'의 connected=false와 미적용 설명은 이전 조사 당시 기록이며, 현재 적용 결과는 이 절을 따른다.
